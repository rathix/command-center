# Command Center Configuration
# All parameters have sensible defaults — override only as needed.
# Precedence: CLI flag > Environment variable > Default

# Server listen address
# LISTEN_ADDR=:8443

# Path to kubeconfig file for Kubernetes API access
# KUBECONFIG=~/.kube/config

# Health check interval (Go duration format: 30s, 1m, etc.)
# HEALTH_INTERVAL=30s

# Data directory for auto-generated certificates
# DATA_DIR=/data

# Log format: "json" (default, for production) or "text" (for development)
# LOG_FORMAT=json

# Custom TLS certificates (leave empty for auto-generated self-signed CA)
# TLS_CA_CERT=
# TLS_CERT=
# TLS_KEY=
